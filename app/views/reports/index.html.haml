%h1 Listing Reports
%table
  %thead
    %tr
      %th Name
      %th SQL
  %tbody
    -reports.each do |report|
      %tr
        %td=link_to report.name, report
        %td=report.sql

=link_to "New Report", new_report_path

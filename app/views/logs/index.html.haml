%h1 Listing Log Entries

.right= link_to 'New Log', new_log_url, class: 'button_button'
=paginate logs
%table
  %thead
    %tr
      %th Logged at
      %th Caller
      %th
      %th
  %tbody
    - logs.each do |logg|
      %tr
        %td= logg.opened_at
        %td= logg.caller
        %td= link_to 'Edit', edit_log_url(logg)
        %td= link_to 'Destroy', logg, :confirm => 'Are you sure?', :method => :delete, :id => "destroy_log_#{logg.id}"

=paginate logs
%br
.right= link_to 'New Log', new_log_url, class: 'button_button'
%br
%br
